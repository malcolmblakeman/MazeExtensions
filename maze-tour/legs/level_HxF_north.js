"use strict";
/* level_HxF (second pass) - north exit on reset boxes. Separate file so it
 * executes AFTER the GxF return (tour order = manifest order). */
window.MB_TOUR = window.MB_TOUR || [];
window.MB_TOUR.push(
  { room: "level_HxF", path: "RDDRRRUURRUU", cross: null, gem: false }, // S11 HxF revisit north exit, reset boxes (folded U)
);
