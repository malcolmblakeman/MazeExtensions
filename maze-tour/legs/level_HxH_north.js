"use strict";
/* level_HxH (second pass) - north exit on reset boxes. Separate file so it
 * executes AFTER the GxH return (tour order = manifest order). */
window.MB_TOUR = window.MB_TOUR || [];
window.MB_TOUR.push(
  { room: "level_HxH", path: "RRRRRRUU", cross: null, gem: false }, // S5 HxH revisit north exit, reset boxes (folded U)
);
